{
  "name": "ecommerce-fullstack",
  "version": "1.0.0",
  "description": "Fullstack project combining Node.js backend and React frontend.",
  "scripts": {
    "start": "concurrently ""npm run start --prefix backend" "npm run start --prefix frontend"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "",
  "license": "ISC"
}
